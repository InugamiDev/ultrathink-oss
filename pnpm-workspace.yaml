# Workspace layout — see docs/CODEBASE-LAYOUT.md for rationale
packages:
  # First-party libraries (apps depend on these)
  - "packages/*"

  # MCP servers (separate deployment shape)
  - "mcp/*"

  # User-facing apps
  - "apps/*"

  # Dev tooling (test harness)
  - "tools/harness"

  # Remotion video project (kept as-is)
  - "videos"
